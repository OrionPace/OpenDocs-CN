# Active upstream projects for the automated GitHub-docs translation pipeline.
#
# Antigravity currently does not expose a public GitHub docs/ tree compatible
# with the old Gemini CLI/Codex sync pipeline, so this list is intentionally
# empty. Manual/curated Antigravity pages live under docs/antigravity/.
projects: []

# Our own repo identifier, used for generated attribution / issue links.
ourRepo: OrionPace/antigravity-doc-zh
